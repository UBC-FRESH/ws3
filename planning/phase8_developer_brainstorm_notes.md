# Phase 8 — Developer Brainstorm Notes (raw)

**Status**: raw input, preserved as written.
**Design doc**: [phase8_embedded_agents.md](phase8_embedded_agents.md)

---

OK good. the model I want to talk about is number 4 in your list above. Here is my idea: I have been trying to design and implement an "ecosystem" of open software modules that can be mixed and matched in various ways to build complex simulation/optimization/statistical models of forest ecosystems, forest sector supply chains, the decisions we make to manage these. 

I have been building these modules such that they are designed to be built, extended, documented, and *operated* (or at least orchestrated) using powerful frontier coding agent technology (as opposed to the human researcher/developer/user having to do everything themselves, which is a real impediment to innovation and deployment in tihs case because the models get big and complex enough very quickly such that *many* of the non-systems-engineer non-computer-scientist people I want to be part of this model-development and deployment conversation are likely to just check out and withdraw from active participation in the conversation if things get too complicated). so i developed `freshforge` and `agent-workbench` and `femic` (see `~/projects/`) to help provide agent-assisted model workflow orchestration, multi-agent team deployment and coordination framework, and model-building/running/interoperability/orchestration, etc. 

so far this has been *working* and i have been able to build bigger/better/faster models than I every previously had (and I have been playing this game for 30 years now)... *but* the reliability of getting agents basically *operate* (or at least *orchestrate* the software is a bit hit-and-miss). `agent-workbench` and `freshforge` and `femic` all *help* to add structure and context and workflows and directives and contracts and layers of planning and penalties and incentives to nudge the multi-agent teams into doing what I *need* them to do more reliably, but I need to fully close the gap so i do not need to micro-manage the agents anymore (which I am good at, but not sure how to train a n00b human developer/user to do). 

so now we get to phast 8 of ws3 planning idea: all the stuff I described above is basically "top down". what if the missing piece of the puzzle is to *also* approach this "bottom up" and then things meet in the middle and... bam! cood stuff happens. 

In this case, whaat if "bottom up" means embedding LLM model agents *inside* and *throughout* all of my software packages in the UBC-FRESH modelling ecosystem? If the locally embedded agents are hard-wired into the software, they should be able to reliably respond to certain requests as long as these requests come through the right API channels of the software they are embedded in. Like, call one of the models I have running in one of my several self-hosted model provider endpoints directly from python code (instead of via an IDE chat UI like we are doing now, which is the "top down approach"). The `agent-workbench` team of coding agents that the actual user interfaces with via an interactive chat UI (to provide developer/user direction and feedback) is where the primary directives come from, the Agent Hub Coordinator then translates the user request into a feasible plan that is compatible with UBC-FRESH software package best-practicees... which I want to include the Coordinator agent *knowning* (because we include instructions in its custom agent profile directives as well as redundant instructions each UBC-FRESH software packge `AGENTS.md` filed, etc.) that the go-to surface for interfacing with all the packages I developed includes "talk to the embedded agent" (who can reliably produce answers or output or advice from the package they are embedded in).

Do you see what I trying to do here?